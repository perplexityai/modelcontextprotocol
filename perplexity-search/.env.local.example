# Perplexity API Configuration
# Required: Get your API key from https://www.perplexity.ai/settings/api
PERPLEXITY_API_KEY=your_api_key_here

# HTTP Server Authentication (Optional)
# Only required when running the HTTP server (pnpm run dev:server)
# Used for Basic Auth when connecting via HTTP/HTTPS
MCP_USER=your_username
MCP_PASS=your_password

# Server Configuration (Optional)
# Uncomment and modify if needed
# PORT=8080